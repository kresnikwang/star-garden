# 星空花园 - 道具融入烟花系统 修改方案

## 1. 设计目标

将当前"独立存在"的道具收集系统与粒子/烟花系统进行深度联动，实现：
- **收集即强化**：收集到的道具会在后续烟花爆炸中"混入"对应的元素
- **可视化反馈**：玩家能直观感受到"我收集的东西正在改变游戏体验"
- **成长感**：收集越多，烟花效果越丰富，形成正向循环
- **主题一致性**：每个道具的特效都符合其主题意象

---

## 2. 核心设计概念：「道具附魔」

每个收集到的道具会为烟花增加一层"附魔"效果。当玩家放烟花时，系统根据已收集的道具列表，按概率在烟花中混入对应的专属粒子。

### 2.1 附魔强度公式

```
附魔概率 = 基础概率(20%) + 收集数量加成(每收集1个+3%) + 连续游玩加成
混入粒子数 = 基础数量(2~5个) × 收集数量系数(1 + 已收集种类数 × 0.15)
```

> 举例：收集了3种道具的樱花主题玩家，每次烟花有约29%概率混入樱花瓣粒子，混入数量约为3~7个。

---

## 3. 道具 → 粒子效果 映射表

### 🌸 春·樱花夜

| 道具 | Emoji | 粒子效果 | 行为描述 |
|------|-------|----------|----------|
| 樱花瓣 | 🌸 | `petal` 粉色花瓣 | 从爆炸点缓慢飘落，带轻微左右摇摆 |
| 花蕊 | 🌺 | `sparkle` 金色光点 | 短暂闪烁后消失，像花粉飘散 |
| 蝴蝶 | 🦋 | `butterfly` 蝴蝶飞舞 | 生成2只小蝴蝶，沿曲线飞离爆炸点 |
| 露珠 | 💧 | `droplet` 水滴 | 向下坠落，带透明渐变和微小弹跳 |
| 花环 | 💐 | `ring` 花环扩散 | 爆炸时额外产生一个彩色圆环向外扩散 |
| 春风 | 🍃 | `wind` 风流 | 产生几条绿色流线，向随机方向吹散 |
| 新芽 | 🌱 | `sprout` 嫩芽 | 从爆炸点下方"生长"出几个绿色小点 |

### 🌊 夏·萤火海

| 道具 | Emoji | 粒子效果 | 行为描述 |
|------|-------|----------|----------|
| 萤火虫 | ✨ | `firefly` 萤火虫 | 黄色光点，带脉动发光，缓慢上升 |
| 海星 | ⭐ | `starfish` 海星 | 星形粒子，旋转着向外飞 |
| 贝壳 | 🐚 | `shell` 贝壳光 | 珍珠白色弧线，像贝壳边缘的反光 |
| 珊瑚 | 🪸 | `coral` 珊瑚枝 | 橙红色小分支，向四周"生长" |
| 水母 | 🪼 | `jellyfish` 水母 | 半透明伞状粒子，缓慢上升并脉动 |
| 海浪 | 🌊 | `wave` 波浪 | 蓝色弧形粒子，像水花四溅 |
| 椰子 | 🥥 | `coconut` 椰果 | 棕色圆点，带抛物线轨迹坠落 |

### 🍂 秋·枫叶谷

| 道具 | Emoji | 粒子效果 | 行为描述 |
|------|-------|----------|----------|
| 枫叶 | 🍁 | `maple` 枫叶 | 红色/橙色叶片形状，旋转飘落 |
| 松果 | 🌰 | `pinecone` 松果 | 棕色小椭圆，带轻微弹跳落地 |
| 橡果 | 🫒 | `acorn` 橡果 | 小圆点，沿抛物线快速坠落 |
| 蘑菇 | 🍄 | `spore` 孢子 | 白色小点，缓慢飘散像孢子粉 |
| 南瓜 | 🎃 | `ember` 余烬 | 橙色光点，像篝火火星飘起 |
| 落日 | 🌅 | `sunset` 霞光 | 橙红色长条光线，向外放射 |
| 暖茶 | 🍵 | `steam` 蒸汽 | 白色半透明粒子，缓慢上升消散 |

### ❄️ 冬·极光雪

| 道具 | Emoji | 粒子效果 | 行为描述 |
|------|-------|----------|----------|
| 雪花 | ❄️ | `snowflake` 雪花 | 六角形/星形，缓慢飘落 |
| 冰晶 | 💎 | `ice` 冰晶 | 透明蓝色菱形，闪烁后碎裂 |
| 极光 | 🌌 | `aurora` 极光带 | 绿色/紫色弧形光带，短暂出现 |
| 雪人 | ⛄ | `snowman` 雪球 | 白色圆球，互相碰撞弹开 |
| 铃铛 | 🔔 | `bell` 铃铛光 | 金色光点，伴随微小闪烁 |
| 星光 | ⭐ | `star` 星光 | 明亮的白色十字星，缓慢闪烁 |
| 暖炉 | 🔥 | `warmth` 暖光 | 橙红色光晕粒子，像温暖气息 |

---

## 4. 技术实现方案

### 4.1 新增/修改的文件

```
app/frontend/src/
├── lib/
│   ├── collectible-effects.ts      # 新增：道具-粒子映射配置
│   └── collection.ts               # 修改：增加获取已收集道具列表的辅助函数
├── components/
│   ├── ParticleCanvas.tsx          # 修改：核心 - 接收collection数据，混入道具粒子
│   └── CollectibleEffectOverlay.tsx # 新增（可选）：收集道具时的特效提示
└── pages/
    └── Game.tsx                    # 修改：将collection数据传入ParticleCanvas
```

### 4.2 核心修改详解

#### (1) 新增 `collectible-effects.ts` - 道具效果配置中心

```typescript
// 定义每个道具对应的粒子生成器
export interface CollectibleEffect {
  emoji: string;
  name: string;
  // 混入概率基础值 (0~1)
  baseChance: number;
  // 粒子生成函数 - 在爆炸点创建对应的粒子
  spawnParticles: (x: number, y: number, theme: ThemeConfig, count: number) => Partial<Particle>[];
}

// 春主题道具效果
export const springEffects: Record<string, CollectibleEffect> = {
  '🌸': {
    emoji: '🌸',
    name: '樱花瓣',
    baseChance: 0.25,
    spawnParticles: (x, y, theme, count) => {
      // 生成粉色花瓣粒子，带飘落物理
      return Array.from({ length: count }, () => ({
        type: 'petal',
        x, y,
        vx: (Math.random() - 0.5) * 2,
        vy: Math.random() * 1 + 0.5, // 缓慢向下
        color: theme.particleColors[Math.floor(Math.random() * 3)],
        size: Math.random() * 4 + 3,
        life: 1, maxLife: 120 + Math.random() * 60,
        rotation: Math.random() * Math.PI * 2,
        rotationSpeed: (Math.random() - 0.5) * 0.1,
      }));
    }
  },
  '🦋': {
    emoji: '🦋',
    name: '蝴蝶',
    baseChance: 0.15,
    spawnParticles: (x, y, theme, count) => {
      // 生成蝴蝶粒子，沿曲线飞行
      return Array.from({ length: Math.min(count, 3) }, (_, i) => {
        const angle = (Math.PI * 2 / 3) * i + Math.random() * 0.5;
        return {
          type: 'butterfly', // 需要新增粒子类型
          x, y,
          vx: Math.cos(angle) * 1.5,
          vy: Math.sin(angle) * 1.5,
          color: '#FFD700',
          size: 6,
          life: 1, maxLife: 180,
          rotation: angle,
          // 蝴蝶特有的正弦波动
          orbitAngle: angle,
          orbitRadius: 2,
          orbitSpeed: 0.15,
        };
      });
    }
  },
  // ... 其他道具类似定义
};

// 夏、秋、冬主题类似定义...
export const collectibleEffectsMap: Record<string, Record<string, CollectibleEffect>> = {
  spring: springEffects,
  summer: summerEffects,
  autumn: autumnEffects,
  winter: winterEffects,
};

// 计算当前已收集道具应混入的效果
export function getActiveEffects(
  themeId: string,
  collected: Record<string, number>
): { effect: CollectibleEffect; count: number }[] {
  const effects = collectibleEffectsMap[themeId];
  if (!effects) return [];

  return Object.entries(collected)
    .filter(([emoji, count]) => count > 0 && effects[emoji])
    .map(([emoji, count]) => ({ effect: effects[emoji], count }));
}

// 计算混入概率
export function calculateMixChance(
  collected: Record<string, number>
): number {
  const uniqueItems = Object.values(collected).filter(c => c > 0).length;
  return Math.min(0.20 + uniqueItems * 0.03, 0.60); // 最高60%
}
```

#### (2) 修改 `ParticleCanvas.tsx` - 核心粒子系统

**A. 新增 Props 接收收集数据**

```typescript
interface ParticleCanvasProps {
  theme: ThemeConfig;
  growth: GrowthState;
  collection?: CollectionProgress; // 新增：可选的收集数据
  // ... 其他不变
}
```

**B. 修改 `createFireworkParticles` 函数**

在标准烟花爆炸逻辑之后，增加道具混入逻辑：

```typescript
const createFireworkParticles = useCallback((x: number, y: number, intensity: number = 1) => {
  // 1. 原有烟花粒子（保持不变）
  const count = Math.floor((20 + Math.random() * 15) * intensity);
  const newParticles: Particle[] = [];
  // ... 原有烟花生成代码 ...

  // 2. 新增：道具混入效果
  if (collection && collection.collected) {
    const activeEffects = getActiveEffects(theme.id, collection.collected);
    const mixChance = calculateMixChance(collection.collected);

    for (const { effect, count: collectedCount } of activeEffects) {
      // 每个已收集的道具有独立概率触发
      if (Math.random() < mixChance * effect.baseChance) {
        // 收集数量越多，混入粒子越多
        const intensityMultiplier = 1 + Math.log(collectedCount + 1) * 0.3;
        const spawnCount = Math.floor((2 + Math.random() * 3) * intensityMultiplier);
        const effectParticles = effect.spawnParticles(x, y, theme, spawnCount);
        newParticles.push(...effectParticles.map(p => ({...baseParticle, ...p})));
      }
    }
  }

  particlesRef.current.push(...newParticles);
}, [theme, collection]);
```

**C. 新增粒子类型支持**

在 `Particle` interface 中增加新类型：

```typescript
type ParticleType = 
  | 'firework' | 'trail' | 'meteor' | 'ambient' | 'swipe' 
  | 'charge' | 'nebula' | 'petal' | 'lighttrail' | 'sparkle' 
  | 'starfield' | 'groundglow' | 'echo' | 'floater'
  // 新增道具相关类型
  | 'butterfly' | 'droplet' | 'ring' | 'wind' | 'sprout'
  | 'firefly' | 'starfish' | 'shell' | 'coral' | 'jellyfish' | 'wave'
  | 'maple' | 'pinecone' | 'acorn' | 'spore' | 'ember' | 'sunset' | 'steam'
  | 'snowflake' | 'ice' | 'aurora' | 'snowman' | 'bell' | 'warmth';
```

**D. 在 `animate()` 循环中增加新类型的物理/渲染**

```typescript
// 在 animate 函数的粒子更新逻辑中增加：

switch (p.type) {
  // ... 原有类型处理 ...

  case 'butterfly':
    // 正弦波动飞行
    p.x += p.vx + Math.sin(p.life * 10) * 0.5;
    p.y += p.vy + Math.cos(p.life * 8) * 0.3;
    p.rotation += 0.05;
    p.life -= 0.003;
    break;

  case 'droplet':
    // 水滴下坠，带微小弹跳
    p.vy += 0.08; // 重力
    p.y += p.vy;
    p.x += p.vx;
    p.vx *= 0.95;
    // 触底弹跳
    if (p.y > canvas.height - 10 && p.vy > 0) {
      p.vy *= -0.4;
      p.y = canvas.height - 10;
    }
    p.life -= 0.004;
    break;

  case 'ring':
    // 圆环扩散
    p.size += 1.5;
    p.life -= 0.015;
    break;

  case 'firefly':
    // 萤火虫缓慢上升 + 脉动
    p.vy -= 0.01; // 轻微浮力
    p.y += p.vy;
    p.x += Math.sin(p.life * 15) * 0.3;
    // 脉动透明度
    p.life -= 0.002;
    break;

  case 'maple':
    // 枫叶旋转飘落
    p.x += Math.sin(p.life * 5 + p.rotation) * 0.8;
    p.y += 0.8;
    p.rotation += 0.03;
    p.life -= 0.003;
    break;

  // ... 其他新类型类似处理
}
```

**E. 在渲染逻辑中增加新类型的绘制**

```typescript
// 在 drawParticle 函数中：

switch (p.type) {
  // ... 原有绘制 ...

  case 'butterfly':
    // 绘制简化蝴蝶形状（两个椭圆翅膀）
    ctx.save();
    ctx.translate(p.x, p.y);
    ctx.rotate(p.rotation);
    ctx.fillStyle = p.color;
    ctx.globalAlpha = alpha;
    // 左翅
    ctx.beginPath();
    ctx.ellipse(-3, -2, 4, 3, 0.3, 0, Math.PI * 2);
    ctx.fill();
    // 右翅
    ctx.beginPath();
    ctx.ellipse(3, -2, 4, 3, -0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    break;

  case 'droplet':
    // 水滴形状
    ctx.fillStyle = p.color;
    ctx.globalAlpha = alpha * 0.7;
    ctx.beginPath();
    ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
    ctx.fill();
    // 高光
    ctx.fillStyle = 'rgba(255,255,255,0.5)';
    ctx.beginPath();
    ctx.arc(p.x - p.size*0.3, p.y - p.size*0.3, p.size*0.3, 0, Math.PI * 2);
    ctx.fill();
    break;

  case 'ring':
    // 空心圆环
    ctx.strokeStyle = p.color;
    ctx.globalAlpha = alpha * 0.6;
    ctx.lineWidth = 2;
    ctx.beginPath();
    ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
    ctx.stroke();
    break;

  case 'snowflake':
    // 六角雪花
    ctx.save();
    ctx.translate(p.x, p.y);
    ctx.rotate(p.rotation);
    ctx.strokeStyle = p.color;
    ctx.globalAlpha = alpha;
    ctx.lineWidth = 1;
    for (let i = 0; i < 6; i++) {
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.lineTo(0, -p.size * 2);
      ctx.stroke();
      ctx.rotate(Math.PI / 3);
    }
    ctx.restore();
    break;

  // ... 其他类型
}
```

#### (3) 修改 `Game.tsx` - 传递收集数据

```typescript
// 在 Game.tsx 中，将 collection 传给 ParticleCanvas
<ParticleCanvas
  theme={theme}
  growth={growth}
  collection={collection}  // 新增
  onGesture={handleGesture}
  // ... 其他 props
/>
```

---

## 5. 可选增强：收集特效提示

在 `Game.tsx` 中，当新收集到道具时，可以显示一个临时提示：

> "🌸 樱花瓣已融入你的烟花！"

这个提示在收集瞬间出现，2秒后消失，让玩家明确感知到"收集改变了什么"。

---

## 6. 方案特点总结

| 特性 | 说明 |
|------|------|
| **向后兼容** | collection 是可选 prop，不传时不影响原有逻辑 |
| **性能可控** | 混入粒子数量有限制（最多每种3~5个），不会突破 MAX_PARTICLES |
| **渐进增强** | 收集越多效果越丰富，但即使只收集1个也有明显视觉变化 |
| **主题统一** | 每个道具的特效都贴合主题意象，不突兀 |
| **代码模块化** | 效果配置独立在 `collectible-effects.ts` 中，方便后续扩展新道具 |

---

## 7. 后续可扩展方向

1. **组合特效**：收集齐一套道具后，触发专属的组合烟花形态（如春日花雨、夏日萤火风暴）
2. **道具专属手势**：收集特定道具后解锁对应的新手势（如收集蝴蝶后解锁"蝶舞"手势）
3. **粒子升级**：同种道具收集10个以上时，对应粒子效果升级（花瓣变大、蝴蝶变多等）
4. **音效联动**：混入道具粒子时播放对应的音效（铃铛声、水滴声等）
